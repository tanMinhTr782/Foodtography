import { Text, View } from 'react-native';
import { RootScreens } from '..';

export const AddRecipeFromSearch = (props: { onNavigate: (string: RootScreens) => void }) => {
    return (
        <View>
            <Text>Add-Recipe-From-Search</Text>
        </View>
    );
};
